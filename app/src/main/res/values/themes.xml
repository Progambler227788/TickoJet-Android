<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TickoJet" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/deep_orange</item>
        <item name="colorPrimaryVariant">@color/deep_orange_dark</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/sky_blue</item>
        <item name="colorSecondaryVariant">@color/sky_blue_dark</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Background and surface -->
        <item name="android:colorBackground">@color/light_gray</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnBackground">@color/dark_gray</item>
        <item name="colorOnSurface">@color/gray</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>

        <!-- Error, success, and warning colors -->
        <item name="colorError">@color/error_red</item>
        <item name="colorSuccess">?attr/colorSuccess</item>
        <item name="colorWarning">?attr/colorWarning</item>


        <!-- Bottom Navigation Style -->
        <item name="bottomNavigationStyle">@style/Widget.TickoJet.BottomNavigationView</item>
        <!-- Default font -->
<!--        <item name="fontFamily">@font/inter</item>-->
    </style>
</resources>
