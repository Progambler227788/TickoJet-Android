<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_height="match_parent"
    android:background="@color/background_light">

    <!-- Search Bar with Glassmorphism Effect -->
    <androidx.cardview.widget.CardView
        android:id="@+id/searchBar"
        android:layout_width="0dp"
        android:layout_height="56dp"
        android:layout_margin="16dp"
        android:radius="32dp"
        app:cardElevation="16dp"
        app:cardCornerRadius="12dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingStart="16dp">

            <!-- Search Icon -->
            <ImageView
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_search"
                app:tint="@color/text_secondary" />

            <!-- Search Input -->
            <EditText
                android:id="@+id/et_search"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:hint="Search bookings..."
                android:backgroundTint="@color/glassmorphism_background"
                android:textColor="@color/text_primary"
                android:textSize="14sp"
                android:padding="10dp"
                android:inputType="text"
                />

            <!-- Filter Button -->
            <ImageView
                android:id="@+id/btn_filter"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:src="@drawable/ic_filter"
                android:background="@drawable/rounded_button_bg"
                android:padding="10dp"
                android:layout_marginEnd="8dp"
                app:tint="@color/deep_orange" />
        </LinearLayout>
    </androidx.cardview.widget.CardView>

    <!-- Horizontal RecyclerView for Bookings -->
    <com.talhaatif.tickojet.custom.StatefulRecyclerView
        android:id="@+id/userBookingsRecyclerView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="8dp"
        app:emptyIcon="@drawable/a1"
        app:emptyText="No trending events found"
        app:errorIcon="@drawable/error"
        app:errorText="Failed to load events"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/searchBar"
        tools:listitem="@layout/rv_booking" />



</androidx.constraintlayout.widget.ConstraintLayout>